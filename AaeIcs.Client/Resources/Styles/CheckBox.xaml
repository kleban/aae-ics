<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style x:Key="CheckBoxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{DynamicResource CheckBoxBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource CheckBoxBorderBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource CheckBoxGlyphForegroundBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid x:Name="TemplateRoot" Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="CheckBoxBorder" 
                                CornerRadius="3" 
                                Padding="1" Margin="1" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <Grid x:Name="MarkGrid">
                                <Path x:Name="OptionMark"
                                      Data="{DynamicResource OptionMark}"
                                      Fill="{DynamicResource CheckBoxGlyphForegroundBrush}" 
                                      Margin="1" Opacity="0" Stretch="None"/>
                                <Rectangle x:Name="IndeterminateMark" Fill="{DynamicResource CheckBoxGlyphForegroundBrush}" 
                                           Margin="2" Opacity="0"/>
                            </Grid>
                        </Border>
                        <ContentPresenter Grid.Column="1" 
                                          x:Name="ContentPresenter" 
                                          Focusable="False" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasContent" Value="true">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Padding" Value="4,-1,0,0"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" TargetName="CheckBoxBorder" Value="{DynamicResource CheckBoxBackgroundHoveredBrush}"/>
                            <Setter Property="BorderBrush" TargetName="CheckBoxBorder" Value="{DynamicResource PrimaryBrandBrush}"/>
                            <Setter Property="Fill" TargetName="OptionMark" Value="{DynamicResource CheckBoxGlyphForegroundBrush}"/>
                            <Setter Property="Fill" TargetName="IndeterminateMark" Value="{DynamicResource CheckBoxGlyphForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" TargetName="CheckBoxBorder" Value="{DynamicResource CheckBoxBackgroundDisabledBrush}"/>
                            <Setter Property="BorderBrush" TargetName="CheckBoxBorder" Value="{DynamicResource CheckBoxBorderDisabledBrush}"/>
                            <Setter Property="Fill" TargetName="OptionMark" Value="{DynamicResource CheckBoxGlyphForegroundDisabledBrush}"/>
                            <Setter Property="Fill" TargetName="IndeterminateMark" Value="{DynamicResource CheckBoxGlyphForegroundDisabledBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" TargetName="CheckBoxBorder" Value="{DynamicResource CheckBoxBackgroundPressedBrush}"/>
                            <Setter Property="BorderBrush" TargetName="CheckBoxBorder" Value="{DynamicResource PrimaryBrandBrush}"/>
                            <Setter Property="Fill" TargetName="OptionMark" Value="{DynamicResource CheckBoxGlyphForegroundPressedBrush}"/>
                            <Setter Property="Fill" TargetName="IndeterminateMark" Value="{DynamicResource CheckBoxGlyphForegroundPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter Property="Opacity" TargetName="OptionMark" Value="1"/>
                            <Setter Property="Opacity" TargetName="IndeterminateMark" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter Property="Opacity" TargetName="OptionMark" Value="0"/>
                            <Setter Property="Opacity" TargetName="IndeterminateMark" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>