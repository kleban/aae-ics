<Page x:Class="AAEICS.Client.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:AAEICS.Client.Views"
      xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
      xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
      syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
      mc:Ignorable="d"
      Title="HomePage">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>


    <Grid>
        <syncfusion:SfDataGrid x:Name="DataGrid" 
                               ItemsSource="{Binding IncomingCertificates}"
                               AutoGenerateColumns="True"
                               ColumnSizer="Star"
                               QueryRowHeight="DataGrid_QueryRowHeight"
                               AutoGeneratingColumn="DataGrid_AutoGeneratingColumn">
        
            <syncfusion:SfDataGrid.CellStyle>
                <Style TargetType="syncfusion:GridCell">
                    <Setter Property="Foreground" Value="#333333" />
                </Style>
            </syncfusion:SfDataGrid.CellStyle>
        
        </syncfusion:SfDataGrid>
    </Grid>
</Page>
