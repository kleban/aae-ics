<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="LaptopLayout">
        <Grid x:Name="BaseGrid" Background="Transparent" Margin="0">
            <ScrollViewer Grid.Row="0" Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <Grid x:Name="Card_Grid"
                      Background="Transparent"
                      Height="590"
                      VerticalAlignment="Top"
                      Margin="10,0,10,0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="c0" Width="1*" />
                        <ColumnDefinition x:Name="c1" Width="1*" />
                        <ColumnDefinition x:Name="c2" Width="1*" />
                        <ColumnDefinition x:Name="c3" Width="1*" />
                        <ColumnDefinition x:Name="c4" Width="1.3*" MaxWidth="260" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="CG_R1" Height="0.30*" />
                        <RowDefinition x:Name="CG_R2" Height="0*" />
                        <RowDefinition x:Name="CG_R3" Height="0.80*" />
                        <RowDefinition x:Name="CG_R4" Height="0.40*" />
                        <RowDefinition x:Name="CG_R5" Height="0*" />
                        <RowDefinition x:Name="CG_R6" Height="0.02*" />
                    </Grid.RowDefinitions>

                    <!--// Card - 1 //-->

                    <ContentControl Grid.Row="0" Grid.Column="0"
                                    Template="{DynamicResource Card1}" />

                    <!--// Card - 2 //-->

                    <!-- <ContentControl Grid.Row="0" Grid.Column="1" -->
                    <!--                 Template="{DynamicResource Card2}" /> -->
                    <!-- -->
                    <!-- ~1~// Card - 3 //@1@ -->
                    <!-- -->
                    <!-- <ContentControl Grid.Row="0" Grid.Column="2" -->
                    <!--                 Template="{DynamicResource Card3}" /> -->
                    <!-- -->
                    <!-- ~1~// Card - 4 //@1@ -->
                    <!-- -->
                    <!-- <ContentControl Grid.Row="0" Grid.Column="3" -->
                    <!--                 Template="{DynamicResource Card4}" /> -->
                    <!-- -->
                    <!-- ~1~// Card - 5 //@1@ -->
                    <!-- -->
                    <ContentControl Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4"
                                    Template="{DynamicResource CardTableDesktop}" />
                    <!-- -->
                    <!-- ~1~// Card - 6 //@1@ -->
                    <!-- -->
                    <!-- <ContentControl Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" -->
                    <!--                 Template="{DynamicResource Card6}" /> -->
                    <!-- -->
                    <!-- ~1~// Card - 7 //@1@ -->
                    <!-- -->
                    <!-- <ContentControl Grid.Row="1" Grid.Column="4" Grid.RowSpan="3" -->
                    <!--                 Template="{DynamicResource Card7Vertical}" /> -->
                </Grid>
            </ScrollViewer>
        </Grid>
    </DataTemplate>
</ResourceDictionary>