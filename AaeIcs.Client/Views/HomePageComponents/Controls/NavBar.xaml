<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ControlTemplate x:Key="NavBar">
        <Border Style="{DynamicResource NavBarStyle}" HorizontalAlignment="Stretch" VerticalAlignment="Center"> 
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Style="{DynamicResource NavBarButtonStyle}" Content="Home"/>
                <Button Grid.Column="1" Style="{DynamicResource NavBarButtonStyle}" Content="New incoming certificate"/>
                <Button Grid.Column="2" Style="{DynamicResource NavBarButtonStyle}" Content="Something"/>
                <Button Grid.Column="3" Style="{DynamicResource NavBarButtonStyle}" Content="Something"/>
            </Grid>
        </Border>
    </ControlTemplate>
</ResourceDictionary>