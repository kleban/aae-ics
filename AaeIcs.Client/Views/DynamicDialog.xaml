<Window x:Class="AAEICS.Client.Views.DynamicDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:AAEICS.Client.Views"
        mc:Ignorable="d"
        Title="Створення об'єкта" Height="350" Width="400" Padding="10"
        Loaded="DynamicDialog_Loaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <ScrollViewer>
            <ItemsControl x:Name="PropertiesList" ItemsSource="{Binding Properties}">
                                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />  
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Bold" />
                            
                            <ContentControl Grid.Column="1">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel  Margin="0,0,0,10">
                                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                                                    <TextBlock Text="{Binding ErrorMessage}" 
                                                               Foreground="Red" 
                                                               FontSize="11" 
                                                               Margin="2,2,0,0"
                                                               TextWrapping="Wrap">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsComplexType}" Value="True">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <ComboBox ItemsSource="{Binding AvailableOptions}" 
                                                                  SelectedItem="{Binding Value}" 
                                                                  DisplayMemberPath="Name" /> </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="1" Content="Зберегти" Width="100" Height="30" 
                HorizontalAlignment="Right" Margin="0,10,0,0" 
                Command="{Binding SaveCommand}"/>
    </Grid>
</Window>
